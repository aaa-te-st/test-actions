name: 'Reusable workflow'
on: 'workflow_dispatch'
jobs:
  test_job:
    name: 'Just a test job'
    runs-on: 'ubuntu-latest'
    steps: 
      - run: echo "This is a test job. This is the job ID ${{ job.container.id }}"
